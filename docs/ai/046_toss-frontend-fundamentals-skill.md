# Toss frontend fundamentals skill

날짜: 2026-06-11

## 이유

프로젝트 범위에서 Toss Frontend Fundamentals 기준을 프론트엔드 코드 리뷰와 구현에 재사용해야 했다.

## 작업

- 이 메모가 포함된 커밋에서 `toss-frontend-fundamentals` 스킬을 `.codex/skills` 아래에 추가했다.
- 원본 스킬의 `SKILL.md`, `rules`, `references`와 MIT `LICENSE`를 포함했다.
- 외부 스킬 문서 원문 보존을 위해 `.codex`를 Prettier 대상에서 제외했다.

## 결과

- `npm run verify` 통과.
