# DB 생명주기 스크립트

날짜: 2026-06-10

## 이유

API 개발 중 Postgres를 백그라운드로 켜고 필요 없을 때 중지하는 루트 명령이 필요했다.

## 작업

- 이 메모가 포함된 커밋에서 `npm run dev:db:stop`을 추가했다.
- 개발 서버 표준 문서에 DB 시작/중지 명령을 반영했다.

## 결과

- `npm run verify` 통과.
