# NMM Template

React, NestJS, shared contract, UI package를 묶은 앱 템플릿이다.

현재 인증은 ID/PW 기반 세션 쿠키를 사용한다.

- 관리자: `admin` / `admin`
- 일반 사용자: `user` / `user`

## 실행

```sh
npm install
npm run dev
```

## 확인

```sh
npm run verify
```
